# TODO

- [x] Inspect existing Projects components (`app/projects/page.tsx`, `ArticleIndex.tsx`, `HoverExpand.tsx`).
- [x] Fix broken Tailwind class names in `app/projects/page.tsx` (corrupted tokens).
- [x] Fix ESLint error in `app/projects/HoverExpand.tsx` by removing `as unknown as any` from animation easing.
- [ ] (Optional) Fix remaining `no-explicit-any` occurrences elsewhere in repo (e.g., `components/Model.tsx`).
- [ ] (Optional) Address performance warning by replacing `<img>` with `next/image` where appropriate.
